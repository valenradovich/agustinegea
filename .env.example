# Optional. No environment variables or accounts are needed to run the site.
# Set to true to enable Vercel Web Analytics in production builds.
# NEXT_PUBLIC_* values are public; never put secrets in them.
NEXT_PUBLIC_ENABLE_ANALYTICS=false
